<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/show_list_height"
    android:background="@drawable/list_season_background"
    android:clickable="true"
    android:divider="@drawable/horizontal_divider"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingLeft="@dimen/show_list_left_padding"
    android:paddingRight="@dimen/default_padding"
    android:showDividers="middle">

    <CheckBox
        android:id="@+id/checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:theme="@style/Theme.MyShows.WhiteControl"
        tools:checked="true" />

    <TextView
        android:id="@+id/season_number"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:fontFamily="sans-serif-light"
        tools:text="3 season" />

    <ImageView
        android:id="@+id/arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_arrow" />

</LinearLayout>
